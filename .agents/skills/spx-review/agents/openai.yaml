interface:
  display_name: "SPX Review"
  short_description: "Full PR flow with code review and merge"
  default_prompt: "Use $spx-review to run the full commit, branch, push, PR, code review, auto-fix, and merge flow."
