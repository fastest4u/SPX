name: Gate 6 post-proof principal actions

on:
  workflow_dispatch:
    inputs:
      candidate_sha:
        description: Immutable release source SHA
        required: true
        type: string
      approval_artifact_id:
        description: Signed Gate 6 approval artifact ID
        required: true
        type: string
      approval_run_id:
        description: Gate 6 approval run ID
        required: true
        type: string
      preclose_artifact_id:
        description: Accepted pre-close evidence artifact ID
        required: true
        type: string
      preclose_run_id:
        description: Accepted pre-close evidence run ID
        required: true
        type: string

permissions:
  contents: read

jobs:
  sign:
    # STAGE-C-PIN: immutable reviewed Stage B map/consumer snapshot.
    uses: fastest4u/SPX/.github/workflows/gate6-postproof-principal-signer.yml@1c25f4b3fac398f30c70d26a94c15cefc6f73d15
    permissions:
      actions: read
      attestations: write
      contents: read
      id-token: write
    with:
      candidate_sha: ${{ inputs.candidate_sha }}
      approval_artifact_id: ${{ inputs.approval_artifact_id }}
      approval_run_id: ${{ inputs.approval_run_id }}
      preclose_artifact_id: ${{ inputs.preclose_artifact_id }}
      preclose_run_id: ${{ inputs.preclose_run_id }}
